const frutas = ["peras", "manzanas", "mandarinas", "ciruelas"];

const resultado = frutas.pop();

// pop devuelve el último elemento de una lista y lo elimina de la misma

console.log(resultado);
console.log(frutas);